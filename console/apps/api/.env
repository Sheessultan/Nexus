PORT=4000
# Listen address — 0.0.0.0 accepts connections from other machines on the network (Windows Firewall may need an inbound rule for PORT).
HOST=0.0.0.0
JWT_SECRET=change-me-in-production
CONSOLE_AUTH_USER=admin
CONSOLE_AUTH_PASSWORD=admin
